sounddevice
Pillow   # tools/build_palette.py's colour extraction (imported by palette_jobs.py, unconditionally - not an optional hardware lib like the ones below)
opencv-python
websockets
numpy
scipy
ai-edge-litert   # runs src/sensing/audio.py's YAMNet .tflite model directly -
                 # mediapipe (the previous choice) has no working Linux
                 # aarch64 build; this does, including for newer Python
PyYAML
paho-mqtt
qrcode
pyserial   # accel_stick.py's USB-serial link to the handheld M5Stack stick,
           # and output/dmx.py's software-timed DMX512 output over a USB-DMX
           # interface - pure Python, works fine on a dev laptop too, not Pi-specific
requests   # weather.py's Open-Meteo HTTP calls